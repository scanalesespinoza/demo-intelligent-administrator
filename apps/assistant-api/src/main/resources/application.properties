quarkus.application.version=${QUARKUS_APPLICATION_VERSION:0.0.1}
quarkus.http.port=${QUARKUS_HTTP_PORT:8080}
quarkus.log.console.format=${QUARKUS_LOG_CONSOLE_FORMAT:${quarkus.log.console.format.default}}
quarkus.log.console.format.default=%d{HH:mm:ss} %-5p [%c{2.}] (%t) %s%e%n
# quarkus.oidc.auth-server-url=${OIDC_ISSUER}
# quarkus.oidc.client-id=assistant-api
# quarkus.oidc.credentials.secret=${OIDC_SECRET}

# iAdmin defaults
iadmin.window.default-minutes=${IADMIN_WINDOW_DEFAULT_MINUTES:15}

# LLM (configurable via env var)
llm.endpoint=${LLM_ENDPOINT:http://localhost/mock-llm}
llm.model=${LLM_MODEL:granite-7b-instruct}
llm.api-key=${LLM_API_KEY:}
